<h2>Add ingredients to <%= @cocktail.name %></h2>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.submit "Submit Ingredients", class: 'btn btn-primary' %>
<% end %>
